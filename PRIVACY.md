# Privacy Policy

cycleview stores user-defined page URLs, names, durations, and timing settings in `chrome.storage.local`.

- Settings data is stored only on the local device.
- No settings are sent to external servers.
- The extension does not collect or send credentials, passwords, or cookies.
- The extension does not use external APIs.

The extension uses Chrome extension APIs to create, switch, reload, and close managed tabs.
